info:
  name: レビュー更新
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/books/3/reviews
  body:
    type: json
    data: |-
      {
        "comment": "更新コメント",
        "rating": 4.5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
