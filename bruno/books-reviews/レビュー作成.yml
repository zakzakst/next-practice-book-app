info:
  name: レビュー作成
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/books/3/reviews
  body:
    type: json
    data: |-
      {
        "comment": "レビューコメント",
        "rating": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
