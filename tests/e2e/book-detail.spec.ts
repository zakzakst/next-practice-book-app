import { expect, test } from "@playwright/test";

test("書籍詳細ページの挙動", () => {
  test.fixme("アクセスした時、URLのIDに対応した書籍の詳細情報が表示される", async () => {});

  test.fixme("URLのIDに対応した書籍がない場合、「お探しの書籍が見つかりません」と表示される", async () => {});
});
