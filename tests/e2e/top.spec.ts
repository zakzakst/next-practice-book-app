import { expect, test } from "@playwright/test";

test("トップページの挙動", () => {
  test.fixme("アクセスした時、キーワード絞り込みされていない書籍一覧の1ページ目が表示される", async () => {});

  test.fixme("キーワード絞り込みした時、絞り込みされた書籍一覧の1ページ目が表示される", async () => {});

  test.fixme("キーワード絞り込みした時、条件に一致する書籍がない場合は「条件に一致書籍が見つかりません」と表示される", async () => {});

  test.fixme("キーワード絞り込みをクリア時、キーワード絞り込みされていない書籍一覧の1ページ目が表示される", async () => {});

  test.fixme("ページネーションが正しく挙動する", async () => {});
});
