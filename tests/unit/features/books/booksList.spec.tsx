import { describe, expect, test, vi } from "vitest";

describe("BooksList", () => {
  test.todo("渡されたデータに対応した内容が表示される", () => {});

  test.todo(
    "カードをクリックした時、対象の書籍IDを引数として関数が発火する",
    () => {},
  );
});
